# Secrets / env
.env

# Virtual env & caches
.venv/
.cache/
*.log

# Large raw dataset (downloaded by src.ml.get_data)
data/creditcard.csv

# Runtime store (regenerated by seed on startup)
data/*.db
data/*.db-wal
data/*.db-shm

# Python
__pycache__/
*.pyc
.pytest_cache/
.DS_Store

# NOTE: models/ and data/sample_transactions.csv are committed so the demo runs
# without the 150 MB dataset download.
